const knex = require('./knex')
const bcrypt = require('bcrypt')
const validator = require('validator')

module.exports = {
/* example
  getUserById(id) {
    return knex('user')
      .where({id})
      .first()
  },
*/
}
